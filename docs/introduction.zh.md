# Open Agent SDK - 项目介绍

[English Version](./introduction.md)

---

## 概述

Open Agent SDK 是一个开源的 TypeScript 框架，用于构建 AI Agent。它提供了类似 Claude Agent SDK 的开发体验，但完全透明且没有供应商锁定。

## 为什么选择它？

### 🔓 开源透明
- 完整的源代码可见性
- MIT 许可证
- 社区驱动开发
- 没有黑盒

### 🔌 供应商无关
- **多供应商支持**：OpenAI、Google Gemini、Anthropic
- **易于扩展**：通过简单接口添加自定义 Provider
- **无供应商锁定**：无需修改代码即可切换 Provider

### 🎯 生产就绪
- **类型安全**：完整的 TypeScript 支持和严格的类型约束
- **高测试覆盖率**：86%+ 代码覆盖率
- **经过实战验证**：核心 Agent 循环在多个 Provider 上验证
- **取消支持**：通过 AbortController 中断操作

## 核心概念

### Agent 循环

SDK 实现了智能体推理模式：

1. **观察**：Agent 接收输入和当前状态
2. **思考**：Agent 推理应该采取什么行动
3. **行动**：Agent 执行工具（文件操作、Shell 命令、网页搜索等）
4. **重复**：循环继续直到任务完成或达到最大轮数

```
用户输入 → [观察 → 思考 → 行动] → 结果
                ↑_________↓
              （循环直到完成）
```

### 工具

工具是 Agent 的能力。SDK 提供 14 个内置工具：

- **文件操作**：读取、写入、编辑文件
- **Shell 执行**：运行 bash 命令，支持超时和后台运行
- **代码搜索**：Glob（查找文件）、Grep（搜索内容）
- **网页访问**：WebSearch、WebFetch
- **任务管理**：委托给子 Agent（包含 TaskList、TaskCreate、TaskGet、TaskUpdate）

### 会话

会话支持持久化对话：

- **创建**：使用 `createSession()` 开始新对话
- **恢复**：使用 `resumeSession()` 从上次中断处继续
- **分叉**：使用 `forkSession()` 创建分支探索不同路径
- **存储**：InMemory（默认）或基于文件的持久化

### 权限

通过 4 种权限模式控制 Agent 可以做什么：

- `default`：破坏性操作（编辑/写入/bash）前询问用户
- `acceptEdits`：自动批准编辑，写入/bash 需要询问
- `bypassPermissions`：自动批准所有操作
- `plan`：生成执行计划但不运行

### Hooks

通过事件钩子扩展 Agent 行为：

- `onTurnStart` / `onTurnEnd`：追踪对话轮次
- `onToolExecute` / `onToolResult`：监控工具使用
- `onPermissionRequest` / `onPermissionDecision`：自定义权限逻辑
- `onStreamChunk` / `onStreamComplete`：流处理
- `onError`：错误处理

## 架构

### 高层概览

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              Open Agent SDK                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   用户代码              核心 SDK                       外部服务              │
│  ┌─────────┐           ┌─────────┐                   ┌─────────┐            │
│  │ prompt()│──────────►│ Agent   │──────────────────►│ OpenAI  │            │
│  └─────────┘           │  循环   │                   │ Google  │            │
│  ┌─────────┐           │         │                   │Anthropic│            │
│  │ Session │──────────►│ ┌─────┐ │                   └─────────┘            │
│  └─────────┘           │ │Tools│ │                                          │
│                        │ │(14) │ │                   ┌─────────┐            │
│                        │ └─────┘ │──────────────────►│  文件   │            │
│                        │ ┌─────┐ │                   │  编辑   │            │
│                        │ │Hooks│ │                   │ 搜索    │            │
│                        │ │(10) │ │                   │  网页   │            │
│                        │ └─────┘ │                   │ 任务    │            │
│                        └────┬────┘                   └─────────┘            │
│                             │                                               │
│                        ┌────┴────┐         ┌─────────┐                      │
│                        │ Session │◄───────►│Storage  │                      │
│                        │ Manager │         │内存/文件│                      │
│                        └─────────┘         └─────────┘                      │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 详细架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Open Agent SDK 架构图                               │
│                              v0.1.0-alpha.0                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ╔═══════════════════════════════════════════════════════════════════════╗   │
│  ║                         公共 API 层                                    ║   │
│  ║  ┌─────────────┐    ┌─────────────────────────────────────────────┐   ║   │
│  ║  │   prompt()  │    │              Session API                     │   ║   │
│  ║  │  (单次调用) │    │  ┌──────────┐ ┌───────────┐ ┌────────────┐ │   ║   │
│  ║  │             │    │  │  创建    │ │  恢复     │ │   分叉     │ │   ║   │
│  ║  └─────────────┘    │  └──────────┘ └───────────┘ └────────────┘ │   ║   │
│  ║                     │  ┌──────────┐ ┌───────────┐ ┌────────────┐ │   ║   │
│  ║                     │  │  发送    │ │  流式     │ │  压缩      │ │   ║   │
│  ║                     │  └──────────┘ └───────────┘ └────────────┘ │   ║   │
│  ║                     └─────────────────────────────────────────────┘   ║   │
│  ╚═══════════════════════════════════════════════╦═══════════════════════╝   │
│                                                  ║                          │
│  ╔═══════════════════════════════════════════════╩═══════════════════════╗   │
│  ║                      核心执行引擎                                       ║   │
│  ║                                                                        ║   │
│  ║   ┌──────────────────────────────────────────────────────────────┐    ║   │
│  ║   │                    Agent 循环                                 │    ║   │
│  ║   │  ┌─────────────┐    ┌─────────────┐    ┌─────────────────┐  │    ║   │
│  ║   │  │    观察     │───→│    思考     │───→│      行动       │  │    ║   │
│  ║   │  └─────────────┘    └─────────────┘    └─────────────────┘  │    ║   │
│  ║   │                              ↑_______________________________│    ║   │
│  ║   └──────────────────────────────────────────────────────────────┘    ║   │
│  ║                                                                        ║   │
│  ║   ┌─────────────┐   ┌─────────────┐   ┌─────────────┐   ┌─────────┐  ║   │
│  ║   │   Provider  │   │   Tool      │   │  Permission │   │  Hook   │  ║   │
│  ║   │   Manager   │   │   Registry  │   │   Manager   │   │ Manager │  ║   │
│  ║   └─────────────┘   └─────────────┘   └─────────────┘   └─────────┘  ║   │
│  ║                                                                        ║   │
│  ╚═══════════════════════════════════════════════╦═══════════════════════╝   │
│                                                  ║                          │
│  ╔═══════════════════════════════════════════════╩═══════════════════════╗   │
│  ║                     Provider 适配器                                    ║   │
│  ║  ┌──────────────┐  ┌──────────────┐  ┌────────────────────────────┐  ║   │
│  ║  │    OpenAI    │  │    Google    │  │         Anthropic          │  ║   │
│  ║  │   Provider   │  │   Provider   │  │        Provider            │  ║   │
│  ║  └──────────────┘  └──────────────┘  └────────────────────────────┘  ║   │
│  ╚══════════════════════════════════════════════════════════════════════╝   │
│                                                                              │
│  ╔═══════════════════════════════════════════════════════════════════════╗   │
│  ║                      工具生态 (14 个工具)                               ║   │
│  ║                                                                         ║   │
│  ║   ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐    ║   │
│  ║   │    Read     │ │    Write    │ │    Edit     │ │    Bash     │    ║   │
│  ║   └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘    ║   │
│  ║   ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐    ║   │
│  ║   │    Glob     │ │    Grep     │ │  WebSearch  │ │   WebFetch  │    ║   │
│  ║   └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘    ║   │
│  ║   ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐    ║   │
│  ║   │  BashOutput │ │   KillBash  │ │  TaskList   │ │  TaskCreate │    ║   │
│  ║   └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘    ║   │
│  ║   ┌─────────────┐ ┌─────────────┐                                     ║   │
│  ║   │   TaskGet   │ │ TaskUpdate  │                                     ║   │
│  ║   └─────────────┘ └─────────────┘                                     ║   │
│  ╚═══════════════════════════════════════════════════════════════════════╝   │
│                                                                              │
│  ╔═══════════════════════════════════════════════════════════════════════╗   │
│  ║              会话管理与持久化                                          ║   │
│  ║                                                                         ║   │
│  ║   ┌──────────────────┐         ┌──────────────────────────────────┐   ║   │
│  ║   │    会话状态      │         │           存储实现               │   ║   │
│  ║   │  ┌────────────┐  │         │  ┌──────────────┐ ┌────────────┐ │   ║   │
│  ║   │  │   空闲     │  │         │  │   内存存储   │ │  文件存储  │ │   ║   │
│  ║   │  │   就绪     │  │◄────────┼──┤  (默认)      │ │ (SQLite)   │ │   ║   │
│  ║   │  │  运行中    │  │         │  └──────────────┘ └────────────┘ │   ║   │
│  ║   │  │   错误     │  │         └──────────────────────────────────┘   ║   │
│  ║   │  │   关闭     │  │                                              ║   │
│  ║   │  └────────────┘  │                                              ║   │
│  ║   │                  │                                              ║   │
│  ║   │  [消息历史] [压缩] [分叉]                                         ║   │
│  ║   └──────────────────┘                                              ║   │
│  ╚═══════════════════════════════════════════════════════════════════════╝   │
│                                                                              │
│  ╔═══════════════════════════════════════════════════════════════════════╗   │
│  ║                   消息类型系统                                         ║   │
│  ║  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────────┐  ║   │
│  ║  │    用户     │ │   助手      │ │  工具结果   │ │   压缩边界      │  ║   │
│  ║  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────────┘  ║   │
│  ╚═══════════════════════════════════════════════════════════════════════╝   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 组件依赖关系

```
                    prompt() / Session API
                           │
                           ▼
                    ┌──────────────┐
                    │  Agent 循环  │
                    └──────┬───────┘
                           │
                 ┌─────────┼─────────┐
                 │         │         │
                 ▼         ▼         ▼
           ┌────────┐ ┌──────────┐ ┌──────────────┐
           │Provider│ │ToolReg   │ │  Permission  │
           │Manager │ │istry     │ │   Manager    │
           └────────┘ └──────────┘ └──────────────┘
                 │         │              │
                 └─────────┴──────────────┘
                           │
                           ▼
                    ┌──────────────┐
                    │ Hook Manager │
                    │  (10 个事件) │
                    └──────────────┘
```

## 使用场景

### 1. 代码助手
```typescript
const result = await prompt("分析这个代码库并提出改进建议", {
  model: 'your-model',
  apiKey: process.env.OPENAI_API_KEY,
  allowedTools: ['Read', 'Glob', 'Grep'],
  cwd: './src',
});
```

### 2. DevOps 自动化
```typescript
const result = await prompt("检查服务器健康状况，如有需要则重启", {
  model: 'your-model',
  apiKey: process.env.OPENAI_API_KEY,
  allowedTools: ['Bash', 'Read'],
  permissionMode: 'default', // 危险操作需要批准
});
```

### 3. 研究助手
```typescript
const session = createSession({
  model: 'your-model',
  apiKey: process.env.OPENAI_API_KEY,
  allowedTools: ['WebSearch', 'WebFetch', 'Write'],
});

await session.send("研究最新的 AI 发展并写一份摘要");
for await (const msg of session.stream()) {
  console.log(msg);
}
```

### 4. 交互式开发
```typescript
const session = createSession({
  model: 'your-model',
  apiKey: process.env.OPENAI_API_KEY,
  hooks: {
    onPermissionRequest: async (request) => {
      // 自定义批准逻辑
      return request.type === 'edit' ? 'approve' : 'reject';
    },
  },
});
```

## 与 Claude Agent SDK 对比

| 功能 | Open Agent SDK | Claude Agent SDK |
|------|---------------|------------------|
| 开源 | ✅ 是 | ❌ 否 |
| 多供应商 | ✅ OpenAI/Gemini/Anthropic | ❌ 仅 Anthropic |
| Agent 循环 | ✅ | ✅ |
| 内置工具 | ✅ 14 个工具 | ✅ 类似 |
| 会话管理 | ✅ | ✅ |
| 权限系统 | ✅ | ✅ |
| Hooks | ✅ 10 个事件 | ✅ 类似 |
| 子 Agent | ✅ | ✅ |
| 浏览器自动化 | 🚧 计划中 | ✅ |
| 结构化输出 | 🚧 计划中 | ✅ |

**功能完成度**：约 70-80%

## 快速开始

1. **安装**
   ```bash
   npm install open-agent-sdk@alpha
   ```

2. **快速示例**
   ```typescript
   import { prompt } from 'open-agent-sdk';
   
   const result = await prompt("当前目录有哪些文件？", {
     model: 'your-model',
     apiKey: process.env.OPENAI_API_KEY,
   });
   
   console.log(result.result);
   ```

3. **下一步**
   - 查看 [API 参考](./api-reference.zh.md)
   - 探索[示例](../examples/)
   - 加入[社区讨论](https://github.com/Octane0411/open-agent-sdk/discussions)

## 社区与支持

- **GitHub**: https://github.com/Octane0411/open-agent-sdk
- **Issues**: https://github.com/Octane0411/open-agent-sdk/issues
- **Discussions**: https://github.com/Octane0411/open-agent-sdk/discussions
- **Twitter**: [@octane0411](https://twitter.com/octane0411)

## 许可证

MIT License © 2026 Octane0411
